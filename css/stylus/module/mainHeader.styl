.mainHeader
  wrapped()
  shadow()
  position fixed
  top 0
  display flex
  width 100%
  align-items center
  z-index 10
  background-color white
  justify-content space-between
  
  +below($desktop)
    wrapped(90%)
    